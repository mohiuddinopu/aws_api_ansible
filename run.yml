-   name: Create AWS instance
    hosts: localhost
    connection: local
    gather_facts: False
    roles:
        - create

-   name: Perform setup
    hosts: ec2_hosts
    sudo: yes
    sudo_user: ec2-user
    roles:
        - setup
